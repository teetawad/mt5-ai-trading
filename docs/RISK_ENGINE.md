# Risk Engine

The MT5 risk engine is authoritative over AI.

Rules include kill switch, valid executable decision, mandatory SL/TP, valid stop distance, minimum R:R, max simultaneous positions, max trades/day, stale quote guard, spread guard, free-margin guard, and risk-based volume sizing.

AI confidence cannot override risk limits.
