# Operations

Use `start-trade.bat` and `stop-trade.bat` for local Windows startup/shutdown. Startup should bring up PostgreSQL, API, trading engine, web app, and then verify MT5 connectivity from the dashboard.

Do not embed MT5 credentials in scripts.
